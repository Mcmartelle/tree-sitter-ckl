{
  "name": "tree-sitter-ckl",
  "version": "1.0.0",
  "description": "Tree sitter grammar for checklist (.ckl) files",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.20.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.23.0"
  }
}
